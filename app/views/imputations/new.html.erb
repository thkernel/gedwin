
<div class="br-pageheader">
      
    </div><!-- br-pageheader -->
<div class="br-pagebody mb-30">
  <div class="br-section-wrapper">
    <div class="headers mg-b-20">
        
    <h2>Nouvelle imputation</h2>
    <hr />
    </div>

    	<% if @arrival_mail.present? %>
      		<%= render 'imputations/partials/arrival_mail_imputation_form', imputation: @imputation %>

      	<% elsif @request.present? %>
      		<%= render 'imputations/partials/request_imputation_form', imputation: @imputation %>

      	<% elsif @document.present? %>
      		<%= render 'imputations/partials/document_imputation_form', imputation: @imputation %>
      	<% end %>

</div>
  </div>
  </div>