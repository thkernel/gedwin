<%= form_with(model: request_company, local: true) do |form| %>
  <% if request_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(request_company.errors.count, "error") %> prohibited this request_company from being saved:</h2>

      <ul>
      <% request_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :request_id %>
    <%= form.text_field :request_id %>
  </div>

  <div class="field">
    <%= form.label :company_name %>
    <%= form.text_field :company_name %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
